# Example Vite environment variables for Gaode/Amap
# Copy this file to `.env.local` or `.env` and fill in your own keys.
# Do NOT commit your local .env files with real keys.

VITE_AMAP_KEY=your_gaode_key_here
VITE_AMAP_SECURITY_CODE=your_security_js_code_here

# Notes:
# - VITE_ prefixed variables are exposed to the client at build time by Vite.
# - You should set HTTP referer restrictions in Gaode/AMap console to limit usage from your domains.